import styled from "styled-components";

export const WrapperItemComponent = styled.div`
    border-bottom: 1px solid var(--primary-fade);
`
export const WrapperItem = styled.div`
    padding: 20px;
    display: flex;
    justify-content: flex-start;
    gap: 10px;
    `