import { Link } from "react-router-dom";
import styled from "styled-components";

export const WrapperAccountComponent = styled(Link)`
    background-color: red;
    border-radius: 4px;
    padding: 10px;
`